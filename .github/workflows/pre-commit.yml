name: Run pre-commit

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches: [main]

jobs:
  pre-commit:
    uses: cpp-linter/.github/.github/workflows/pre-commit.yml@main
